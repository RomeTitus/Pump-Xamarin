<?xml version="1.0" encoding="UTF-8"?>

<ContentView
    x:Class="Pump.Layout.Views.ViewBluetoothSummary"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:embeddedImages="clr-namespace:EmbeddedImages;assembly=Pump">
    <ContentView.Content>
        <StackLayout x:Name="stackLayoutBluetoothDeviceSummary" Margin="0,0,0,5">
            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer x:Name="StackLayoutViewSubControllerTapGesture" />
            </StackLayout.GestureRecognizers>
            <StackLayout Orientation="Horizontal">
                <Image
                    x:Name="BlueToothDeviceImage"
                    HeightRequest="40"
                    HorizontalOptions="StartAndExpand"
                    Source="{embeddedImages:ImageResourceExtention Pump.Icons.SwitchController.png}" />
                <Label
                    x:Name="LabelBluetoothDeviceName"
                    FontSize="Large"
                    HorizontalOptions="Start"
                    Text="Controller at small dam"
                    VerticalOptions="Center" />
                <Label
                    x:Name="LabelRSSI"
                    FontSize="Large"
                    HorizontalOptions="EndAndExpand"
                    Text="Lan"
                    VerticalOptions="CenterAndExpand"
                    WidthRequest="100" />
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>